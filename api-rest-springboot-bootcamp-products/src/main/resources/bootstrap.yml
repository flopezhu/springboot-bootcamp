spring:
  application:
    name: api-rest-springboot-bootcamp-products
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: admin
      #profile: info
      enabled: true
debug: false
